extends layout 

block content 
  h1=title
  p Estes são os pets cadastrados no nosso banco de dados: 
  br
  ul 
    each pet in pets 
      li
        a(href=`/registry/pet/${pet._id}`) #{pet.name}